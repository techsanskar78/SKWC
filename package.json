{
  "name": "skwc",
  "version": "1.0.0",
  "private": true,
  "description": "Shri Krishna Wedding Collection — Next.js frontend + Google Sheets backend",
  "scripts": {
    "dev": "npm run dev --prefix deploy/frontend",
    "build": "npm run build --prefix deploy/frontend",
    "start": "npm run start --prefix deploy/frontend",
    "lint": "npm run lint --prefix deploy/frontend",
    "install:all": "npm install --prefix deploy/backend && npm install --prefix deploy/frontend"
  }
}
